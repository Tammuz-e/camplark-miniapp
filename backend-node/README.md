# 找嘢营 简易后端（Node.js）

**无需 Java、Maven、MySQL**，仅需 Node.js 即可本地预览。

## 启动

```bash
cd backend-node
npm install
npm start
```

服务地址：http://localhost:8080/api

默认管理员：admin / admin123

数据存在内存中，重启后重置。仅供本地开发预览，正式环境请使用 Java 版后端。
